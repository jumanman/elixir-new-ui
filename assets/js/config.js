// API配置（使用相对路径，所有复杂逻辑由Worker处理）
const API_CONFIG = {
    BASE_URL: ''
};

const API_ENDPOINTS = {
    GET_APKS: '/api/get_apks',
    UPLOAD: '/api/upload',
    UPDATE: '/api/update',
    LOGIN: '/api/login'
};